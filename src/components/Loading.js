import React from "react";
import { StyledLoading } from "./styles/Loading.styled";

const Loading = () => {
  return (
    <StyledLoading>
      <h2>Loading...</h2>
    </StyledLoading>
  );
};

export default Loading;
